'use strict';
const a = '1' //create variable a
console.log(4 + +a) // concatination int and srt to int
console.log(4 + a) // concatination int and str like str

let incr = 1,
    decr = 1;

incr++ //+1 to variable 'incr'
decr-- //-1 to variable 'decr'

console.log(++incr, --decr)

console.log(2*4 == '8') //true
console.log(2*4 === '8') //false

// operator '&&' mean and
// operator '||' mean or

