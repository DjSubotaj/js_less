"use strict"; //wirte for writing in last version

const a = 12; //creat constant variable
let b = 2; //create variable

b = 3; //'let' we can change after in different to 'const'

console.log(a, b); //output in console 2 variable

const obj = { //creat some obj with some information
    a : 50
};

obj.a = 10; //recreate variable inside obj

console.log(obj);

{
    let res = 50; //create some code block
}

// we can call 'res' only inside {} console.log(res);

const storeName = '<ddqqqqqqqqqqq';

const storeDescription = {
    budget:10000,
    employees:["Bill","Kill","2"],
    products:{"товар": "цена","товар1": "цена"},
    open:true
};

alert('hello');

const answer = prompt('18?','1');
console.log(typeof(answer));
